# Optional: only used if OpenSearch security plugin is enabled.
# For this repo security is disabled by default, but keeping this variable
# avoids bootstrap errors if you toggle security settings in compose.
OPENSEARCH_INITIAL_ADMIN_PASSWORD=ChangeMe_Str0ngP@ssw0rd
